const WAKEUP_MESSAGE = `It's time to get up and go to the museum.`;

const CONTROLS_MESSAGE = `Press any key to get up, then use the arrow keys to move.`;

const CAR_WAITING_MESSAGE = `Your driver is waiting to drive you to the museum. Just approach the passenger side to get in and leave.`;
let seenCarWaiting = false;
const CAR_WAITING_MESSAGE_TWO = `Your driver is still waiting. You should probably get moving.`;

const MOMA_MESSAGE = `Here you finally are at the Museum of Modern Art in New York, ready to present your performance work "The Artist Is Present".`;
let seenMOMAMessage = false;
const MOMA_MESSAGE_TWO = `Now is not the time for wandering around. Get in there and start the performance!`;